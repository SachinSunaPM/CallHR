{
  "name": "@callhr/web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json && vite build",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@callhr/contracts": "file:../../packages/contracts",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  }
}
